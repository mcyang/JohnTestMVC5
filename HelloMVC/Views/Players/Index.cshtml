@model List<HelloMVC.Models.PlayersViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GetEnumerator().Current.player.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetEnumerator().Current.player.Height)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetEnumerator().Current.player.Weight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetEnumerator().Current.player.Age)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetEnumerator().Current.TeamName)
        </th>
        <th>Operation</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.player.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.player.Height)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.player.Weight)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.player.Age)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.player.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.player.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.player.ID })
        </td>
    </tr>
}

</table>
